<UserControl x:Class="OxiumInstaller.Views.InstallPathPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="INSTALLATION PATH"
                   Style="{StaticResource SectionTitle}"/>

        <TextBlock Grid.Row="0"
                   Text="Choose where to install Oxium."
                   Style="{StaticResource SectionSubtitle}"
                   Margin="0,28,0,0"/>

        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,20,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Text="{Binding InstallPath, UpdateSourceTrigger=PropertyChanged}"
                         VerticalAlignment="Center"/>

                <Button Grid.Column="1"
                        Content="BROWSE"
                        Style="{StaticResource LinkButton}"
                        Command="{Binding BrowseCommand}"
                        Margin="8,0,0,0"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Margin="0,16,0,0">
            <TextBlock Text="{Binding DiskSpaceInfo}"
                       Style="{StaticResource StatusLabel}"/>
            <TextBlock Text="Required space: ~300 MB"
                       Style="{StaticResource StatusLabel}"
                       Foreground="{StaticResource OxiumTextMutedBrush}"/>
        </StackPanel>
    </Grid>
</UserControl>
